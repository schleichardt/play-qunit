# Application dependencies

require:
    - play
    - play -> qunit

repositories:
  - LocalModules:
        type:       local
        artifact:   ${application.path}/../[module]
        contains:
                    - qunit