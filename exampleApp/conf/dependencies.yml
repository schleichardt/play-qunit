# Application dependencies

require:
    - play
    - local -> qunit 1.1

repositories:
  - LocalModules:
        type:       local
        artifact:   ${application.path}/../[module]
        contains:
                   - local -> *